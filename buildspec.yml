version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12  # Temporary old version to pass validation
    commands:
      - echo "Installing Node.js 18..."
      - curl -fsSL https://rpm.nodesource.com/setup_18.x | bash -
      - yum install -y nodejs
  pre_build:
    commands:
      - echo "Installing dependencies..."
      - npm install
  build:
    commands:
      - echo "Running build..."
      - npm run build
  post_build:
    commands:
      - echo "Build completed!"

artifacts:
  files:
    - '**/*'
  base-directory: 'dist'
